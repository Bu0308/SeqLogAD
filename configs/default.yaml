# Research Freeze v1.1 convention skeleton. Not a runnable experiment config.
project:
  name: seqlogad
  experiment_id: EXP-YYYYMMDD-NNN
  seed: 42

dataset:
  name: TODO
  version: TODO
  fingerprint: TODO
  raw_path: TODO
  processed_path: TODO

protocol:
  contract: configs/protocols/protocol-v1.1.yaml
  empirical_status: NOT_RUN

parsing:
  parser: drain3
  fit_partition: BASE_TRAIN
  fit_normal_only: true
  state_artifact: TODO

sequence:
  strategy: TODO
  window_size: TODO
  grouping_key: TODO

split:
  strategy: raw_chronological
  base_train: 0.60
  fusion_train_reserved: 0.10
  val_expert: 0.10
  val_fusion_reserved: 0.10
  test: 0.10
  physical_test_guard: TODO

methods:
  must: [unseen_event_type, sequence_length, count_vector, markov_ngram]
  should: [isolation_forest]
  conditional: [transformer, localization, simple_fusion]

scoring:
  aggregation: TODO
  threshold_strategy: TODO_VALIDATION_ONLY
  primary_metric: PR_AUC

output:
  root: outputs
  run_dir: outputs/runs/EXP-YYYYMMDD-NNN

logging:
  level: INFO
