# Unit tests

Active tests cover dataset integrity and the synthetic-only `SCHEMA-001` canonical event/template contract. Remaining parser, sequence, model, retrieval, fusion and downstream files are placeholders. Tests must use small fixtures and must not download data or call external APIs.
