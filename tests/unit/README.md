# Unit tests

Active tests cover dataset integrity, the synthetic-only `SCHEMA-001` canonical event/template contract, and the synthetic-only `SCHEMA-002` partition/sequence/localization/mutation contract. Remaining parser, sequence-builder, model, retrieval, fusion and downstream files are placeholders. Tests must use small fixtures and must not download data or call external APIs.
