# Evaluation

The implemented responsibilities are the preserved v1.1 deterministic raw
five-way structural splits, layered identities, reconciliation validators and
physical TEST guards. Both real TEST artifacts remain sealed and unopened.

The forward v2 responsibilities are leave-one-architecture-out fold audits,
target-buffer label isolation, expert/equal-weight/adaptive-fusion comparisons,
robustness scenarios and replayable evidence reports. The former orderless and
Markov comparisons remain historical v1.1 controls.

Conditional complementarity must measure correlation, disagreement, error overlap, oracle gain, and marginal contribution before any fusion branch opens.

Environment, dataset-integrity, protocol, schema, split, and synthetic seal
tests currently run; no scientific evaluation result exists.
