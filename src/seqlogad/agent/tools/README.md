# Agent tools

The future read-only tool catalog includes anomaly inspection, normal-sequence retrieval, incident retrieval, documentation search, test retrieval, surrounding-log queries, sequence comparison, evidence verification, test recommendation and report creation.

Tool contracts must define inputs, outputs, error behavior, timeout, result limits and evidence provenance.
