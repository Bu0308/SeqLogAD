# Agent

`FUTURE_OUTSIDE_V1_1_CORE`. A future read-only investigation workflow may consume only frozen, versioned detector evidence; it is not an anomaly detector.

It may retrieve evidence, compare observed/expected sequences, propose and verify hypotheses, return `INSUFFICIENT_EVIDENCE`, and recommend regression tests. It has no shell, production write, remediation, training, or model-update tool.

Provider/framework dependencies and implementation remain deferred until the scientific core is complete and a new scope decision is approved.
