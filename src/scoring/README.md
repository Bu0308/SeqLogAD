# Scoring

Normalizes detector outputs into event-level and sequence-level anomaly results.

Input: model scores or transition surprises.

Output: mean, max, top-r and percentile/static threshold results.

Dependencies: NumPy/scikit-learn will be added only when DET-001 starts.

Planned files: `aggregation.py`, `thresholds.py`.

Implementation status: placeholder only.
