# API

Future thin service layer for ingestion, parsing, sequence building, detection, retrieval, investigation, test generation, feedback and health checks.

Input: validated HTTP request schemas.

Output: versioned response schemas and consistent errors.

Dependencies: FastAPI and httpx are intentionally deferred until API-001.

Planned file: `main.py` and route modules.

Implementation status: no API server exists.
