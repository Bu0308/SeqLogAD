# P2.PRE — Base Model / Tokenizer / Revision Freeze

Authoritative machine-readable contract: [base-freeze-v1.yaml](../../configs/models/base-freeze-v1.yaml).
Status: **BLOCKED**; P2.1 execution_authorized=false. Metadata policies are frozen;
gated metadata/access evidence is incomplete. This is not model/GPU validation.

This resume reuses the [upstream verification record](../references/P2.PRE-upstream-verification.json)
from 2026-09-09. No literature search or metadata fetch was repeated. The record
contains official response hashes, package metadata and the two HTTP 401 results.
The existing immutable model/tokenizer commit is verified; it is not the blocker.

## A. Verified upstream facts

- Official pretrained, non-Instruct identifier: `meta-llama/Llama-3.1-8B`.
  Both model and tokenizer use commit `d04e592bb4f6aa9cfee91e2e20afa771667e1d4b`.
  The [revision-specific official API](https://huggingface.co/api/models/meta-llama/Llama-3.1-8B/revision/d04e592bb4f6aa9cfee91e2e20afa771667e1d4b)
  returned that SHA, `LlamaForCausalLM`, BF16 parameter metadata (8,030,261,248),
  tokenizer filenames and manual gating. This proves repository identity, not a weight download.
- Meta documents an autoregressive transformer with grouped-query attention and
  128K context. The contract interprets this as 131,072 tokens; the exact pinned
  config scalar remains an authenticated metadata check.
  [Meta model card](https://github.com/meta-llama/llama-models/blob/main/models/llama3_1/MODEL_CARD.md).
- The API exposes BOS `<|begin_of_text|>` and EOS `<|end_of_text|>`. Numeric IDs,
  resolved tokenizer class and full config contents were not verified: requests
  for pinned `config.json` and `tokenizer_config.json` returned HTTP 401.
- Hugging Face access requires the account to request/receive model access and
  authenticate for gated files. Approval is not established by public metadata.
  [Official gated-model documentation](https://huggingface.co/docs/hub/models-gated).

The [Llama 3.1 Community License](https://github.com/meta-llama/llama-models/blob/main/models/llama3_1/LICENSE)
permits specified use/modification subject to its terms. Distribution obligations
include providing the agreement, applicable attribution/NOTICE, “Built with Llama”
and Llama-prefixed naming for distributed models covered by the relevant clause.
The AUP and additional commercial condition for qualifying >700M-MAU entities
also apply. Owner acceptance/eligibility is not inferred. SeqLogAD's stricter
repository policy stores configs/hashes/metadata only; base weights and adapter
binaries remain external. Any future adapter publication requires its own review.

## B. Literature-supported design and boundaries

The previous targeted search considered 2026, then 2025, then 2024 guidance and
the necessary original method. No newer unrelated method is adopted in this resume.

| Evidence | Supported use here | Boundary |
|---|---|---|
| [Hugging Face, Beyond LoRA, June 2026](https://huggingface.co/blog/peft-beyond-lora) | PEFT comparisons require matched conditions and multiple cost/quality measures | Tooling benchmark/blog; does not prove the winning log-AD architecture |
| [Hugging Face training tooling, December 2025](https://huggingface.co/blog/hf-skills-training) | Reproducible hosted training tooling exists | No hosted job or agent delegation is adopted here |
| [HF Llama 3.1 release guidance, 2024](https://huggingface.co/blog/llama31) | Transformers support for this family | Not proof of our selected full environment or VRAM |
| [QLoRA, NeurIPS 2023 proceedings](https://proceedings.neurips.cc/paper_files/paper/2023/hash/1feb87871436031bdc0f2beaa62a049b-Abstract.html) | Frozen quantized base with low-rank adaptation; NF4/double quantization motivate memory savings | Peer-reviewed; no SeqLogAD or Llama-3.1 memory/quality guarantee. DOI verification was unavailable; no unverified DOI is asserted |
| [Official PEFT quantization guide](https://huggingface.co/docs/peft/v0.18.0/developer_guides/quantization) | NF4/double quantization and k-bit preparation are supported implementation patterns | Hyperparameters in examples are not SeqLogAD choices |

Choosing QLoRA as the common default is a **SEQLOGAD-SPECIFIC ENGINEERING DECISION**,
informed by that evidence and Colab practicality. It is not a literature-proven
equivalence between quantized and full-precision SeqLogAD experts.

## C. Frozen project decisions

**Base and adapters.** Semantic and Sequence reference share the same immutable
upstream base and quantizer configuration. Adapter weights, optimizer state,
resume lineage and checkpoints are independent. A quantized runtime representation
is not byte-identical to BF16 source weights. Only one adapter is active per forward;
no shared activations or free second inference are assumed.

**Tokenizer.** Load `AutoTokenizer` from the pinned repo/commit with `use_fast=True`,
`trust_remote_code=False`. Use existing EOS as padding without extending vocabulary;
right-pad training, left-pad generation. Mask padding by attention-mask positions,
not every EOS token ID. Disable automatic special-token insertion; add one existing
BOS per independent context and EOS only at a genuine completed unit boundary.
Never invent EOS at a truncation boundary. No chat template or EventID-specific
tokens. Vocabulary/embedding mutation requires a separately justified amendment.
Literal special-token text must not silently become control tokens.

**Context.** Upstream capability is 128K. SeqLogAD practical cap is 2,048 tokens
including specials; low-memory cap 1,024; preflight begins at 512. Actual semantic
and sequence horizons remain P2.1/P2.2 decisions. No silent tokenizer truncation:
construct bounded units/windows, record dropped spans and coverage, preserve order,
sessions and partition boundaries. A larger cap requires a versioned amendment.

**Precision.** Prefer native BF16, checked with
`torch.cuda.is_bf16_supported(including_emulation=False)`.
FP16 is conditional on a successful finite-loss/gradient preflight with GradScaler
and FP32 trainable adapter parameters. On overflow/NaN/Inf, stop and use suitable
BF16 hardware; do not silently alter the experiment. FP32 remains for sensitive
operations/master parameters as specified by k-bit preparation; no full-FP32
training fallback. PyTorch warns that BF16-pretrained models can overflow FP16.
[Pinned CUDA implementation](https://raw.githubusercontent.com/pytorch/pytorch/v2.9.1/torch/cuda/__init__.py),
[AMP documentation](https://docs.pytorch.org/docs/2.9/amp.html).

**Training modes.** Default `QLORA_NF4_DOUBLE_QUANT`; low-memory
`QLORA_NF4_DOUBLE_QUANT_LOW_MEMORY`. Both use 4-bit NF4, nested quantization,
uint8 storage, resolved BF16/conditional FP16 compute, gradient checkpointing and
disabled training KV cache. The low-memory profile uses micro-batch 1 and <=1,024
tokens. Record effective batch, accumulation and context differences; do not pool
mode/precision results silently. Standard 16-bit LoRA or 8-bit loading is not an
automatic fallback. No adapter/trainer is implemented here.

**PEFT.** Use Hugging Face PEFT additive low-rank adapters, `bias=none`, immutable
base and embeddings, no saved/trainable base modules. Explicit target-module
allowlists are finalized before owning-task runs. Canonical checkpoints remain
unmerged `adapter_model.safetensors` plus `adapter_config.json`; never overwrite
the base. [Official checkpoint format](https://huggingface.co/docs/peft/developer_guides/checkpoint).

**Runtime.** Python `>=3.12,<3.13`, Linux x86_64, PyTorch CUDA 12.8 (`cu128`), one
GPU, SDPA. Fixed direct versions already verified from PyPI metadata:
torch 2.9.1, transformers 4.57.6, peft 0.18.1, accelerate 1.12.0,
bitsandbytes 0.49.2, tokenizers 0.22.2, safetensors 0.7.0,
huggingface-hub 0.36.2. Each official version URL and requirements are in the
verification record. This deliberately retains a compatible Transformers 4.x
baseline after inspecting newer releases; it does not claim to be the latest stack.
Selected direct constraints/Python markers agree; full transitive resolution,
imports and GPU kernels remain P2.1 preflight. Existing macOS `requirements.lock`
is not a Linux CUDA lock. Resolve/save a full Linux lock and install report before
training; incompatible Colab preinstalls require a matching environment, not silent
upgrades. No datasets/TRL/flash-attn/DeepSpeed/vision/audio dependency is added.
[Official PyTorch wheel matrix](https://pytorch.org/get-started/previous-versions/),
[bitsandbytes 0.49.2 requirements](https://huggingface.co/docs/bitsandbytes/v0.49.2/installation).

**Compute envelope.** Preferred candidate: native-BF16 Ampere-or-newer GPU with
24 GB VRAM; 48 GB offers headroom. A 16 GB T4-class device is a conditional
low-memory candidate, not a measured minimum or fit guarantee. All are project
planning estimates. Weight-only arithmetic (~16.06 GB BF16 or ideal ~4.02 GB at
4 bits) excludes activations, unquantized layers, logits, optimizer and overhead.
No SeqLogAD VRAM, training-time or throughput measurement exists.

**Artifacts.** Preserve `OUTPUT_ROOT=outputs/phase2` and
`<fold_id>/<expert_id>/<run_id>/`. Save config, training/allowed validation metrics,
logs, manifest, environment, dependency lock/install report, checksums and separate
checkpoints. Record base/tokenizer revisions, seed, data manifest hash, code commit
and dirty diff, timestamp/hardware, mode/precision/quantizer, adapter config and
resume/RNG/optimizer lineage. Model cache stays external. No real run directory is
created; `planned_artifact` remains a future metadata-only freeze receipt.

**Colab-first notebook/data contract.** Every future Phase-2 task performing real
training must supply a Colab-ready `.ipynb`; RunPod is secondary. Python modules
remain authoritative. P2.PRE does not create a training notebook.

```text
CANONICAL DATA ROOT: data/phase2/
FUTURE COLAB DATA_ROOT: /content/phase2
FUTURE P2.1 NOTEBOOK: notebooks/P2.1_train_semantic_expert.ipynb
```

Upload the prepared, self-contained `data/phase2/` contents into `/content/phase2`,
preserving manifests and `folds/<fold_id>/{semantic,sequence,graph}/`. Data is not
created yet. Keep all four previously planned notebook paths unchanged. Near the
top expose DATA_ROOT, OUTPUT_ROOT, frozen model configuration, HF_TOKEN secret
mechanism, SEED, mode and resume checkpoint. Use Colab Secrets or an environment
secret for the approved HF account; never print or persist the token in artifacts.
Mount durable output storage or export checkpoints before Colab runtime loss.

SOURCE_LABEL_STATUS remains **UNRESOLVED_DENY**; no representation training,
selection, calibration or evaluation permission is broadened.

## D. Still-open decisions and exact unblock procedure

Essential blockers: authenticated reads of the two pinned metadata files, their
hashes/resolved tokenizer class, and owner license/access confirmation. No need to
repeat the public model-ID/commit research.

On an account approved for the official repository, confirm license acceptance
and access without sharing credentials. In an authenticated environment, fetch
only `config.json` and `tokenizer_config.json` at the frozen commit (no snapshot or
weight download); record SHA-256, architecture, context and tokenizer class/special
token settings. Reconcile them with this contract and record review. Any mismatch
blocks completion. Only after these mandatory fields resolve and tests pass may
P2.PRE become PASS and routing authorize P2.1. Do not start P2.1 as part of closure.

Intentionally open future hyperparameters: LoRA rank/alpha/dropout/learning rate,
target-module subset, actual horizons and expert-specific objective/unit/crop rules.
They must be frozen by the owning task before experiments; they do not justify
inventing metadata or treating a code example as an experimental decision.

## E. Future P2.1 preflight measurements

Before training: verify authenticated revision/metadata agreement, Linux dependency
lock/imports, license/access, CUDA kernels, native BF16 or conditional FP16,
immutable base and isolated trainable adapters. Smoke-load remains P2.1 work.
Measure load and steady-state peak allocated/reserved VRAM with CUDA synchronization
and reset_peak_memory_stats; record loss/gradient finiteness, OOM, wall time,
throughput, effective batch, context/rank/targets and available headroom. Use only
permitted source data after manifest checks. Save failed configurations as failures.

## Resume/verification record

Resume baseline: commit `35ff75e`, clean working tree. Earlier metadata/policy work
and API evidence reused. This resume supplies this missing document, Colab defaults,
blocked-state synchronization and focused guards. No new research or model loading.
No pre-edit expensive suite repeated on resume; prior-session results are historical.

Executed after the final test addition:
- `.venv/bin/python -m pytest -q tests/test_base_freeze_contract.py tests/test_phase2_architecture_contract.py tests/test_phase2_roadmap_migration.py tests/test_excel_plan_authority.py tests/test_active_state_contract.py tests/protocol/test_phase1_artifacts.py` — **90 passed in 1.98s**.
- `.venv/bin/python -m pytest -q` — **366 passed in 3.50s**.
- `.venv/bin/python scripts/extract_excel_roadmap.py --check` — **CURRENT**.
- `.venv/bin/python scripts/build_reference_registry.py --check` — **CURRENT**.
- `git diff --check` — exit 0, no output.

The workbook status-only amendment changes exactly `Task Register!K13` from
`Not started` to `Blocked`; historical P2.0.1 workbook/projection and original
migration remain immutable. The base contract records the new hash chain.
Workbook before/after status views were rendered and visually checked.

P2.PRE = BLOCKED; P2.1 execution_authorized = false. Source-label scope remains
UNRESOLVED_DENY. No model/data/notebook/expert/fusion implementation, weights,
training, sub-agent, commit or push in this resume. Tests validate contracts and
recorded dependency constraints, not an installed GPU training environment.
