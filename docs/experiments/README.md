# Experiment documentation

This directory will contain human-executed v2 cross-architecture run reports, result tables, failures, robustness scenarios, and threats-to-validity notes. Planned conditions and status live in [`../../configs/experiments/cross_architecture_fusion.yaml`](../../configs/experiments/cross_architecture_fusion.yaml) and the v2 plan.

No model experiment has run. Empty metric fields mean `NOT_RUN`, not zero. Final TEST execution is human-owned.

The v1.1 statistical specification [`../statistical-decision-contract.md`](../statistical-decision-contract.md) remains frozen historical provenance. It does not make v2 experiment placeholders runnable or bypass v2 gate and target-label controls.
