# P2.0.1 — Phase-2 Roadmap / Task-ID Migration

Decision authority: explicit researcher request, 2026-09-09. Namespace:
**P2_ARCH_V1**. Migration complete; P2.PRE completion **BLOCKED / NOT_STARTED**;
P2.1 Semantic Expert **execution_authorized=false**. Phase 3 disabled.
No new scientific result or G0–G4 signature is asserted.

## Authority and provenance

The editable authority is `Bang_ke_hoach_SeqLogAD.xlsx`, not its YAML projection.
`scripts/extract_excel_roadmap.py` reads the workbook; it does not generate it.
The workbook's seven sheets, 32 task slots, five gates and 11 source entries remain.
Its eight Phase-2 rows now contain one prerequisite and seven implementation tasks.

The [migration ledger](../../configs/protocols/phase2-roadmap-migration-v1.yaml)
records exact before/after hashes, 102 changed content cells, the historical
crosswalk and unchanged P2.0 section hashes. Executive task-count formulas remain
formulas; the Must-count cache recalculates because a new required comparator
occupies a formerly Should task slot. Conditional REP-DG-001 is retained within
P2.1 and is not silently promoted to a mandatory alignment technique.

Previous status-only amendment PLAN-AMEND-001 is still verified from the signed
G0 workbook to the archived pre-migration workbook. The new migration is a separate
authorized scientific-plan amendment, not a forged G0 signature. Original
`g0-signatures.yaml` and `G0-receipt.json` are unchanged byte-for-byte.
The receipt's old workbook/projection hashes resolve to the two archived files
listed in the migration ledger. Historical P2.1 in that receipt means base freeze.
Future read-only receipt derivation emits P2.PRE; all 19 G0 criteria are unchanged.

## Exact crosswalk and order

| Old namespace ID | P2_ARCH_V1 destination | Retained responsibility |
|---|---|---|
| P2.1 | P2.PRE | Base/tokenizer/license/resource metadata; runtime smoke-load retained as P2.1 preflight |
| P2.2 | P2.1 | Semantic adapter training |
| P2.3 | P2.1 | Conditional source alignment / domain-adversarial diagnostic, REP-DG-001 |
| P2.4 | P2.1 | Semantic scoring/evidence |
| P2.5 | P2.2 | Independent Sequence-LoRA reference training |
| P2.6 | P2.2 | Ordered context and sequence scoring/evidence |
| P2.7 | P2.4 | Structural-temporal GTAT expert |
| P2.8 | P2.5 | Common evidence contract |
| P3.1 diagnostic responsibility | P2.6 | Diversity/scientific evaluation ownership; P3.1 remains a G2 handoff check |

New P2.3 adds the lighter sequence comparator; P2.6 owns diversity evaluation;
P2.7 closes Phase 2 and packages researcher G2 evidence. No sequence winner is chosen.
Historical IDs must be accompanied by their namespace or workbook hash.

```text
P2.PRE — Base Model / Tokenizer / Revision Freeze
→ P2.1 — Semantic Expert
→ P2.2 — Sequence Reference Framework
→ P2.3 — Lightweight Sequence Expert
→ P2.4 — GTAT Expert
→ P2.5 — Common Evidence Contract
→ P2.6 — Expert Diversity / Scientific Evaluation
→ P2.7 — Final Phase-2 Freeze
→ researcher G2 review
```

P2.PRE depends on completed P1.8/G0 and the completed P2.0/P2.0.1 contract work.
Each subsequent task depends on the preceding task. This ordering does not erase
transitive data/interface dependencies. Dependencies alone never grant execution.
Only PRE metadata work is authorized. P2.1 remains blocked until reviewed PRE PASS
and an explicit routing update; no automatic training permission is introduced.

## P2.PRE definition and current blocker

Authoritative requirements: [base-freeze-v1.yaml](../../configs/models/base-freeze-v1.yaml).
Expected output: `LLM-BASE-001`, planned at
`reports/phase2/base-freeze/LLM-BASE-001.json` (not created).

Required concrete metadata:

- Base model identifier and exact immutable upstream model revision/commit.
- Tokenizer identifier and exact immutable tokenizer revision/commit.
- Explicit quantization policy (including none), precision/dtypes and context limit.
- Compatible PEFT/LoRA, transformers, torch, accelerate and optional quantization
  dependency constraints, with lockfile/provenance hashes.
- Model license/official terms, access requirements and verification evidence;
  no credentials stored in repository artifacts.
- GPU/memory assumptions and fair comparison budget.
- Versioned artifact/checkpoint naming, seeds, model/tokenizer provenance URLs,
  environment/determinism policy and reproducibility metadata.

These fields remain null until the next task verifies them; Llama-3.1-8B family
intent is not an exact revision freeze. No invented commit or "main/latest" can
satisfy PASS. The metadata manifest requires researcher review and hashing.
No weights are downloaded for PRE. The old runtime smoke-load requirement remains
mandatory in P2.1 preflight before training; PRE's metadata-only PASS must not be
reported as successful GPU/model execution.

## Source-label blocker and unchanged portability

C2 remains **UNRESOLVED_DENY**. No new label API scope or permission was added.
It blocks supervised source metrics in P2.6 and required dependent scientific
evidence in P2.7/G2, hence the P3.1 handoff and learned fusion requiring G2.
It does not prevent metadata work or future authorized label-free expert work.
No source normal-label filtering, supervised selection, target-label use,
threshold selection or calibration permission is introduced.

Canonical planned root: `data/phase2/` — **NOT CREATED YET**.
Views remain `data/phase2/folds/<fold_id>/semantic/`, `sequence/`, `graph/`.
The full self-contained `data/phase2/` bundle will later be uploaded to Colab or
RunPod with its manifests and allowed fold views; evaluator labels remain outside it.

Future required notebooks, unchanged and not created:

- `notebooks/P2.1_train_semantic_expert.ipynb`
- `notebooks/P2.2_train_sequence_reference.ipynb`
- `notebooks/P2.3_train_lightweight_sequence_expert.ipynb`
- `notebooks/P2.4_train_gtat_expert.ipynb`

## Targeted inspection and changes

Inspected only workbook/projection/extractor, active-state/default/model config,
P2.0 architecture contract and playbooks, primary-context/task-routing, the README
active-state line, notebook README, G0 ledger/receipt routing and related tests.
Continuity records and the spreadsheet skill were read for session/edit requirements.
Full-suite failures additionally required the reference-registry generator/task
mapping and the purge-decision test's active-routing assertion to be inspected.
No full-repository reread, raw-corpus discovery, or literature re-audit was performed.

Changed project files:

- Workbook and `configs/plan/excel-roadmap-v1.yaml` projection.
- `configs/active-state.yaml`, `configs/default.yaml`,
  `configs/models/domain_adaptive_fusion.yaml`.
- `configs/protocols/phase2-architecture-v1.yaml` and the new migration ledger.
- New `configs/models/base-freeze-v1.yaml` requirements.
- `.ai/primary-context.md`, `.ai/task-routing.md`, root `README.md` status line.
- P2.0 architecture document/task playbooks and `notebooks/README.md` routing note.
- This migration record and archived pre-migration workbook/projection.
- `src/seqlogad/protocol/freeze.py`: future receipt routing only; no P1 criteria change.
- `tests/test_phase2_roadmap_migration.py`, `tests/test_phase2_architecture_contract.py`,
  `tests/test_excel_plan_authority.py`, `tests/test_active_state_contract.py`,
  `tests/protocol/test_phase1_artifacts.py`.
- `tests/test_purge_decision_contract.py`: active next-task assertion only.
- `scripts/build_reference_registry.py`: GTAT task-ID references only;
  regenerated `docs/references/reference_registry.yaml` and `ACTIVE_REFERENCES.md`.
- Minimal `_agent_ops` continuity records, listed in the closure receipt.

The extractor, original G0 ledger/receipt, Phase-1 contracts, experts/data access
profiles and P2.0 portability/notebook sections were not changed by this task.
Pre-existing unrelated working-tree edits remain intact.

## Verification receipt

Before: `.venv/bin/python -m pytest -q` → **352 passed in 3.60s**;
`.venv/bin/python scripts/extract_excel_roadmap.py --check` → **CURRENT**.

Focused final command:

```bash
.venv/bin/python -m pytest -q tests/test_phase2_roadmap_migration.py tests/test_phase2_architecture_contract.py tests/test_excel_plan_authority.py tests/test_active_state_contract.py tests/protocol/test_phase1_artifacts.py tests/test_purge_decision_contract.py tests/test_reference_registry.py
```

Result: **103 passed in 2.46s**. The initial narrower focused run passed 83 tests;
the first broader suite then found three stale task/reference mappings
(3 failed, 356 passed in 3.92s). Those mappings were corrected at their authority.

Final broader command `.venv/bin/python -m pytest -q`: **359 passed in 3.88s**.
`.venv/bin/python scripts/extract_excel_roadmap.py --check`: **CURRENT**.
`.venv/bin/python scripts/build_reference_registry.py --check`: **CURRENT**.
`git diff --check`: **exit 0**.

Workbook was rendered before/after with artifact-tool and visually inspected on
all changed sheets. Read-only workbook QA found no formula error cells, all
32 playbook IDs unique, and unchanged merges/tables/validation rules. No unexpected
content/formula edits outside the explicit whitelist. Phase-2 data/notebooks absent.
Tests verify frozen P1 hashes, all 19 unchanged G0 criteria, both archive bindings,
task crosswalk/order, metadata-only prerequisite, disabled P2.1/Phase3 and unchanged
P2.0 expert/data/notebook/label sections. They do not certify model/GPU correctness.

## Scope and exact next task

No model trained/downloaded; no Phase-2 dataset or training notebook created;
no expert/fusion implemented; no Phase 3 started; no commit or push.
Single-agent execution: no sub-agents created or used, no work delegated, one
continuous agent context performed inspection, edits, tests, verification and reporting.

Next: **P2.PRE — Base Model / Tokenizer / Revision Freeze**.
