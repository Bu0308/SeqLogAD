# Dataset manifests

Version-controlled JSON manifests are written here only after raw files are present. A manifest contains relative paths, sizes, SHA-256 digests and a deterministic dataset fingerprint. No HDFS/BGL manifest exists yet because the datasets have not been downloaded.
